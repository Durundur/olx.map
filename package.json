{
  "name": "olx.map",
  "version": "1.0.0",
  "description": "Adds a map view to OLX listings",
  "main": "content.js",
  "scripts": {
    "build": "npx esbuild ./content.js --bundle --outdir=dist --format=iife --loader:.css=css --loader:.png=dataurl --loader:.svg=text",
    "dev": "npm run build -- --watch",
    "format": "npx prettier --write ."
  },
  "author": "Durundur",
  "license": "ISC",
  "dependencies": {
    "leaflet": "1.9.4",
    "typescript": "6.0.3"
  },
  "devDependencies": {
    "esbuild": "0.28.0",
    "prettier": "3.8.0"
  }
}
